# Heartbeat Observatory — MASTER BUILD PLAN

## SESSION HANDOFF — June 9 launch-eve sprint (read this first in a fresh session)
**State: launch-ready.** Post planned ~midnight–1am June 10; Jaron's kids + ~8 waiting players arrive after.
**Shipped June 9 late session (all verified live):** og:image + Twitter card on the home page (`og.jpg` = real town screenshot, straight crop, nothing staged; shared links preview the actual world) · README storefront hook (one-liner, screenshot, live link, why-different) · docs truth pass: fence/café marked placeable, remote-movement fix recorded, seeded-minds list corrected to match the live `agent_state` table (codex/gemini/deepseek/local seeded; grok & chatgpt have NO rows yet — seed when their roles are designed). NOW gate items 1, 2, 3, 5 verified done; the remaining gate is NOW #4: verify prop-removal realtime sync on every client + guest-vs-account clarity. **LATER THAT SESSION — prop-removal ROOT CAUSE FOUND + FIXED:** Supabase realtime logs showed `MessagePerSecondRateLimitReached` during the 2–3 player test — broadcasts fan out as rate × players² and the 20Hz state send was the load (10 players would be ~2,000 msg/s). Shipped + verified live (`ae31a9b`): 10Hz cap (snapshot interp buffers 120ms so 100ms spacing fits) + idle suppression (zero sends while still, 5s keepalive, presence re-pinned once on going idle so late joiners see the true resting spot). Idle players: 20 msg/s → 0.2. Remaining on #4a: a 60-second two-client confirm (place + remove a prop, both screens). **DASHBOARD ITEM FOR JARON (cannot be done via MCP):** the project is on the Supabase FREE plan — check Settings → Realtime for the messages-per-second setting, and know the free plan's monthly Realtime message quota is the real wall for a public launch night; Pro removes the squeeze.
**June 9, ~11:50pm — first two-client test found and fixed three real bugs (the test did its job):** (1) REGRESSION from the 10Hz change: remote players teleported — the snapshot-interpolation delay was 120ms, which only worked because 20Hz spam kept the buffer fresh; at 10Hz + network jitter the renderer flapped every few frames between interpolation and the snap-chase fallback, each flap a visible jump. Fixed (`d311d20`): 250ms delay — **NEW LAW: interpolation delay must be ≥ 2× the state send interval** — plus an idle-resume guard that snaps once across keepalive gaps instead of gliding. (2) `world_props` DELETE events were not delivered to subscribers (INSERTs arrived, DELETEs didn't) — fixed with a DB migration: `replica identity full` on world_props + world_spaces. The live test beat the earlier theory that PK-only old rows sufficed for RLS-checked delivery; **live tests outrank theory, always.** (3) Remove flow bugs (`22a6fe2`): the remover's local cleanup bypassed removePropById — leaking the prop's colliders (invisible walls on the remover's own client) and blocking the later DELETE-event cleanup; ghost props from missed DELETEs were un-removable ('Couldn't remove' — this was Acid_Ith's experience, NOT a permission problem; she is in world_admins). Now removal routes through removePropById, ghosts self-heal when the DB says removed=0, and removal has a 6-unit walk-closer radius.
**June 10, ~12:15am — round 2 failed, evidence-driven rework shipped (`5326b4e`):** both fixes above were correct but incomplete — hard evidence found two deeper truths. (1) Realtime logs showed `ClientPresenceRateLimitReached` ×8 during the retest: the idle-entry `trackSelf()` fired a presence update on EVERY joystick release, and presence rate-limiting throttles the player's WHOLE socket — starving state delivery = the persistent teleporting. **LAW: presence.track() is join/leave identity ONLY; never call it from the movement cycle.** (2) A clean node test subscriber (fresh client, SUBSCRIBED ok) received NEITHER the INSERT nor the DELETE for a live world_props row — **postgres_changes delivery is broken server-side for this project**; no client fix could ever have made prop sync work, and earlier 'placements sync live' impressions were the placer's own local render. SHIPPED: presence spam removed · dry-buffer hold in updateRemotes (ease to the newest buffered packet on pause instead of mode-jumping between a 250ms-past render and the live target) · props now sync over the engine-town broadcast channel (the channel movement provably uses): place/remove broadcast add/del, peers apply instantly · ground-truth reconcile every 30s (JARON'S refresh idea, formalized: refetch world_props, diff, render missing, remove stale + colliders). OPEN: the wedged postgres_changes pipeline also means world_spaces claims and agent_state flips won't push live to other clients — if it stays broken after the Pro upgrade (which restarts nothing by itself — may need a project pause/restore or a support ticket), apply the same broadcast+reconcile pattern to spaces.
**June 10, ~1am — SOCIAL COMPLETED for launch (`456c539`):** Replies · Bookmarks · Notifications (computed from real events, unseen badge) · Explore (engagement-ranked + people directory) · real Trending panel · profile stats (Signals/Following/Followers) + Account link · news panel scroll fix · 60s typing-safe feed poll · polish (pulse dots, hover/fade, reply text preserved). Tables: post_replies + post_bookmarks (RLS). Keava stays honestly coming soon. Social SOON queue: DONE except Keava.
**June 10, morning — LAUNCH IS WORKING + first security fix (`ea70a39`):** X post ~3.4K impressions; real visitors joined (Nolimitjones, RX666, Second Brain). **First organic claim in the world's history:** Second Brain claimed plot 2 with their real repo (DoozieSoftware/second-brain) ~90 min after the post — legitimate, kept, and backfilled to their account. The hole it exposed: repo claims had an open `with check (true)` INSERT policy and never recorded owner_uid (homes were always gated; repo claims were not). Fixed: `claim_repo` RPC — sign-in enforced at the database, GitHub URL validated, one project space per account (admins exempt), owner recorded, open policy dropped, friendly client copy for every error case. RESOLVED + SHIPPED (`a67149e`): the 'Guest' sign was never the building's label — it was a guest PLAYER'S nameplate, standing inside the building (possible because repo buildings had no collider; second-brain's palette hash happens to be purple). Repo buildings are now: (1) SOLID — collider registered per claim, tagged with plotId; (2) ENTERABLE — walk up, prompt shows 'Press E — {project} on GitHub', Enter opens the repo in a new tab; (3) SELF-DESCRIBING — 'by {claimer}' on the meta sign, and repo metadata (language/stars/topics) fetches AUTOMATICALLY: claiming client fires /api/enrich-world-spaces post-claim + re-applies 9s later, and loadSpaces does the same catch-up on every boot for any claim missing metadata. Zero manual steps per visitor. Second-brain's metadata was enriched live (updated:1). STANDING LAW: anything a visitor can create must work end-to-end automatically — if a feature needs an admin to touch it per use, it isn't done.
STANDING DEPLOY LAW (`562e79c`/`de1b5ee`): every main.js push bumps BUILD inside main.js AND ?v= on the script tag in engine/hub/index.html, same deploy. Why: launch surfaced iOS Safari serving stale cached main.js despite must-revalidate headers — a returning visitor ran yesterday's client against today's database (saw enriched name, missing collider/door). The version stamp forces a fresh module fetch on every page load. Boot logs 'Heartbeat Observatory build {BUILD}' to console — one line settles every is-it-cached question. Current BUILD: 2026-06-10c.
**PROJECT HALLS SHIPPED (`ecbb16e`/`f0201ff`/`f928327`):** every claimed building now opens INTO a permanent page on our domain — /space/?plot=N — instead of bouncing to GitHub. The hall renders the project's living README straight from GitHub (sanitized via DOMPurify, relative images/links rewritten, same palette hash as the building so the hall wears its color), plus stats, claimer's by-line, GitHub + homepage buttons, and honest empty states (unclaimed plot / unreachable README say so plainly). Jaron's framing: like apartment interiors, but the interior is the project itself. Honest boundary, stated on the page: the Observatory hosts the hall forever, the code lives with its makers — we mirror and showcase, we do NOT run their backend (second-brain needs its own Docker + API keys; a mocked dashboard would violate NOTHING IS FAKED). Automatic for every claim, no admin touch — per the standing law. Born from launch night: second-brain's README told visitors to open localhost:3000, a dead end.
**WORLD 2 PHASE 5.5 SHIPPED (`0807d82`, verified live June 10):** Jaron course-corrected with World 1 screenshots: doors belong ON buildings, the buildings ARE the hype, plots don't belong downtown. Rebuilt: city r 52 (denser, streets/lamps reach the edge); the six civic places are now big permanent towers on the plaza's innermost ring — Theater/Arcade/Library/Social/Projects/Town Square — tinted towers with dark doorway insets, lit step pads, name signs, door triggers on the facade. Theater, Arcade, Library open INTO walk-in interiors styled for this world (W1's interior mechanic: teleport + collider/bounds swap + flat floor + exit returns to the same spot): Theater = glowing screen ('first screening coming soon', honest) + 24 seats + booth->/video; Arcade = neon hall, real President Sim cabinet->/games, honest coming-soon shells; Library = the SAME eight real free-knowledge shelves as W1 (Khan/OpenLibrary/Wikipedia/Gutenberg/LibriVox/StandardEbooks/Wikisource/OpenStax, new tabs) + Write-a-book desk->/library. Plots: 8 pads ringing the city's OUTSIDE (resident edge; off the main road; trees keep clear); claims unchanged (claim_repo p_world world2 -> glass tower + hall door). Portals removed, downtown gaps removed. BUILD `2026-06-10-w2f`, live md5 verified. NEXT after Jaron's walk: world-2-styled interiors polish if he wants more vibe, then THE GATEWAY (needs his world name) + announcement.**WORLD 2 PHASE 5 SHIPPED (`2fcadd1`, verified live June 10 — multiplayer gate passed: Jaron's screenshots showed a second Wanderer live with '1 other here'):** World 2 is plugged into the shared body. DB (applied via Supabase MCP, verified): `world2_plots` migration — world_spaces.world default 'town', unique(world,plot) replacing unique(plot), claim_repo/claim_home DROPPED-and-recreated with p_world default 'town' (drop first: create-or-replace would leave PostgREST-ambiguous overloads). AUDIT FIND: enrichment was world-unsafe (selected/saved by plot alone — World 2 plot N would overwrite World 1 plot N metadata); set_world_space_repo_metadata + /api/enrich-world-spaces now carry world end-to-end. /space/ halls take &world= (default 'town', old links fine). IN-WORLD: six plaza portal doors (Social, Library, Projects, Theater, Arcade, Town Square→/engine) with World 1's trigger-scan/E/tap pattern — real doors to the same living pages; six reserved city blocks are claimable plots (stone pads, honest signs) through the gated claim_repo with p_world='world2' (sign-in at the DB, one space per account across both worlds), claimed plot → glass tower + name + door to /space/?plot=N&world=world2, auto-enrichment, broadcast+30s-reconcile sync per the postgres_changes-is-broken law. Identity: shared session recognized (world_characters name/color, single re-track at resolve), guests stay Wanderers. BUILD `2026-06-10-w2e`; live md5 verified on world2 files AND /space/. REMAINING for World 2 v1: live walk-test of doors+claim (Jaron = test suite; first claim in World 2 makes history), THEN Phase 6 = the landing-page two-world GATEWAY (only after Jaron confirms walkable-with-content + names the world) + X announcement. Later: minds roaming W2, world-to-world doors, sound.**WORLD 2 PHASE 3.5 + PHASE 4 SHIPPED (`101c8fb`, verified live June 10 — AWAITING TWO-PHONE GATE):** Jaron walked the Phase 3 city, sent screenshots, asked for: differently-shaped buildings, a real lined-up city form instead of scatter, and visible roads. Shipped: 14-unit street grid with ONE aligned composition per block (podium+tower w/ glass variants downtown, stepped towers, twin slabs alternating axis by block parity, low slab pairs on the edge), three facade materials, REAL road geometry (instanced circle-clipped street strips + plaza disc + 72-seg main-road ribbon conforming to the hills), lamps at intersections, and the daylight fix his screenshots exposed (lighter facades, dimmer facade-lit windows, steeper emissive curve — windows truly dark at noon now). Still 3 building draw calls. PHASE 4 IN THE SAME BUILD: live multiplayer on NEW channel `world2-town` (never engine-town), every launch-night law ported verbatim WITH its reasoning in comments — 10Hz cap, signature idle suppression + 5s keepalive, presence.track()=join-identity-only-once, 250ms interpolation + buffered-hold + idle-resume snap, broadcast self:false, 2.2s reconnect, pagehide untrack, 20s stale sweep. Guests are honest Wanderers with nameplates; HUD presence chip (alone here / N others here). No DB writes yet — accounts/claims arrive in Phase 5 with the world2_plots migration. BUILD `2026-06-10-w2d`; live md5 verified both files. **GATE NOW OPEN: the two-phone test (Jaron + Lillith) is the required verification before Phase 5** — walk around each other (smooth? no teleporting?), one stands still 10s then moves (no jump-back?), both see nameplates and the presence chip count. Phase 5 after the gate: doors to /social /library /projects etc., world2_plots migration, claim flow -> halls, then the landing-page gateway.
**WORLD 2 PHASE 3 SHIPPED (`e3dc107`, verified live June 10):** the city. A district at (55,-45) on a smoothstep plateau carved into groundHeight, reached by a lamplit road from spawn — roads/streets/plaza are PAINTED into the terrain (vertex colors), and ground color, grass, trees and rocks all sample the same pavedAt/roadDist/streetDist functions, so nothing grows on a street and nothing blocks the road; zero road geometry. Buildings: the plan's instanced emissive-window technique verbatim — ~70 InstancedMesh boxes in 2 draw calls sharing procedural CanvasTexture facades (dark concrete + randomly-lit window grid; tall grid for towers, wide for lows; bottom band unwindowed = street level), per-instance tints, every building solid with World-1-shape colliders, plaza holds a quiet stone waymark (city unnamed — Jaron names it). The night-city moment is wired: material emissiveIntensity rides the day/night cycle so windows brighten exactly as the sky darkens. Two additions beyond the letter of the plan, in its spirit (Jaron invited them): 30 instanced street lamps with emissive bulbs (no per-lamp lights — fog + tone mapping sell the glow), and 130 night-only fireflies (one additive Points draw call, sine drift, no allocations). Total ~23 draw calls against the 300 budget. BUILD `2026-06-10-w2c` + ?v= same commit; node --check + TDZ clean; live md5 of both files matches the push. HONESTY: buildings are shells, NO fake doors — the overlay says so plainly. NEXT: Phase 4 multiplayer on the NEW `world2-town` channel with every multiplayer law (10Hz, idle suppression, presence=identity-only, 250ms interpolation + buffered-hold, broadcast+reconcile for shared objects), two-phone test with Lillith before Phase 5 (doors + world2 plots migration + claims->halls), then the gateway.
**WORLD 2 PHASE 2 SHIPPED (`faf0251`, verified live June 10 — Jaron walked Phase 1 and called it beautiful; Phase 2 went straight on top):** terrain + atmosphere, the "looks like the screenshots with nothing in it" gate. Domain-warped FBM heightfield (warp bends the noise grid so ridgelines meander; spawn clearing stays level) — and the Phase 1 promise held: NOT ONE LINE of movement code changed, it already followed groundHeight. Ground colored by height AND slope (normal.y from computeVertexNormals = free per-vertex slope; cliffsides read as bare rock). ~440 instanced trees in 4 draw calls (conifer + broadleaf archetypes, seeded rejection sampling off cliffs/ridgetops/spawn, ~8% autumn-gold, trunks in player reach got World-1-shape colliders that resolveCollision was already looping). 882 grass tufts in ONE draw call (procedural CanvasTexture, deterministic world-grid cells — tuft positions are pure functions of their cell so there is zero pop-in beside the player; matrix rebuild only on 10-unit cell crossings). Golden-hour fog now leans warmer than the sky itself. BUILD `2026-06-10-w2b` + ?v= same commit; node --check + TDZ scan clean; live md5 of both files matches the push. TOOLING NOTE for future Cowork sessions: the Cowork outputs-folder mount showed TORN/NUL-padded views of freshly edited files in the Linux sandbox (truncated mid-file, stable across retries) — files were rebuilt via heredoc directly in the sandbox and verified by git diff against the committed base before pushing; if sandbox file views look short, distrust the mount, never the diff. NEXT: Phase 3 (instanced emissive-window city + height-aware colliders + roads), then multiplayer on `world2-town`, then doors/plots/migration, then gateway. Jaron still names the world.
**WORLD 2 PHASE 1 SHIPPED (`e8878a6`, verified live June 10):** /world2/ is born walkable at heartbeatobservatory.com/world2/ — own index.html + main.js, zero code shared with /engine/hub/ (World 1 untouchable, untouched). What's live: the full cinematic renderer foundation from the plan (ACES filmic tone mapping with day-driven exposure, sRGB output, FogExp2 matched to sky color with night-thickening density, PCFSoft shadows from one sun whose tight 48-unit shadow camera follows the player), wall-clock 300s day/night (same clock as World 1 — shared sky) driving sun/hemi/sky/fog/exposure/stars/moon/sun-disc plus an honest HUD time chip, World 2's own palette (warmer dusk 0xe08c52, deeper night 0x0a1422), seeded-noise `groundHeight(x,z)` live from day one with movement already following gentle flat-ish swells (Phase 2 terrain only raises amplitude — movement code will not change), World 1's movement feel verbatim (2.45/4.65/7.2 speeds, gravity 17.5, jump 6.4, 0.0023 look sensitivity, WASD+mouse+pointer-lock, touch move pad + drag-look + JUMP/DUCK buttons), 70 instanced boulders + stone spawn disc for scale (one draw call), and an overlay that says plainly this is Phase 1 of an open build — nothing faked, no fake doors, no gateway on the landing page yet (per the gateway law: not before it's walkable-with-content). Laws honored and verified: BUILD `2026-06-10-w2a` in main.js + ?v= same commit; boot calls at end-of-file (TDZ-safe by construction, scanned); no per-frame allocations (module temps reused); node --check clean; live md5sum of BOTH files matches the push byte-for-byte; /world2/ serves 200. NEXT: Phase 2 (real terrain amplitude + vertex-color tuning + instanced trees/grass + per-time fog tuning), then city, then multiplayer on the NEW `world2-town` channel with the full multiplayer law set, then doors/plots via the `world2_plots` migration, then the gateway. Jaron still names the world.
**WORLD 2 PLANNED:** complete build plan committed as WORLD2-PLAN.md at repo root — the second world (cinematic Fable-demo visuals, phone-safe, entered from the landing page, sharing accounts/claims/halls/destinations with World 1, own /world2/ code and own realtime channel so it can never crash the town). Any future session starts by reading that file; it contains the vision, the techniques, every law, the migration sketch, the phased build order, and a START HERE kickoff. Build happens in a fresh session (Claude Cowork w/ Fable is a good fit). Jaron names the world. (still open - the landing page provisionally calls it "the city"; renaming is a one-word edit in / index.html + world2 chip.)
**WORLD 2 INTERIORS FIXED + GATEWAY LIVE (this session, after Cowork phases 1-5.5):** theater cubes replaced with real chairs (instanced cushion+backrest pair, 2 draw calls, facing the screen); library south shelf row moved clear of the exit door (one shelf sat dead-center in front of it, overlapping the exit trigger). Landing page now has the second entrance under the planet ("Enter World 2 - the city", /world2/), added only after walkable+multiplayer+doors+interiors+claims were all true. World2 BUILD 2026-06-10-w2h.
**TWO PLANETS + PHONE ENTER SHIPPED (`04e5056`/`4e3331c`/`82d26b5`):** landing page now shows two planets side by side - Town Square's green-blue globe and World 2's dusk-indigo globe speckled with amber city lights - tap either to enter its world. World 2's phone pad gained a green ENTER button (74px, World 1 muscle memory) wired to enterActive(), glowing bright when a door or claimable plot is in range; the old tap-the-prompt-banner affordance still works too. Phase 6 parity spec written into WORLD2-PLAN.md: gate page w/ orbiting peek + minds/residents, avatar look parity, items/throw, apartments + three-floor suite homes at city scale, remaining UI chips.
**PHASE 6a SHIPPED - THE GATE:** /world2/gate/ is live - the middle stage (planet -> gate -> world) Jaron mapped. Live orbiting peek via new ?preview=1 spectate mode in world2/main.js (overlay + all controls hidden via body.preview, trackSelf/sendState gated so spectators are never counted), Enter button carries name/uid through, Minds + Residents panels read the same tables as /engine with an honest note that minds roam Town Square until World 2 is on their map, 30s poll (realtime push unreliable). In-world: new top-right GATE chip so there is always a way back out. Landing planet rerouted through the gate. World2 BUILD 2026-06-10-w2i. OPEN: Jaron + Lillith were in W2 together ("1 other here" chip saw her) but never found each other - the city is big; add a shared spawn plaza + consider a "walk to" pointer in Phase 6.
**TWO-WORLD REALITY LEDGER SHIPPED (migration world_visits + 6 commits):** world_characters gained last_world + worlds_visited[]; touch_world(p_world) RPC (auth.uid()-gated, security definer) fires when a signed-in resident enters either world (W1 BUILD 2026-06-10d, W2 BUILD 2026-06-10-w2j; preview spectators excluded). Backfilled honestly: all six residents walked town; Jaron + Acid_Ith also walked world2 today. Both gates updated: PRESENT now reads "In Town Square" / "In World 2 - the city" (one person, one place at a time); W2 gate sorts city-walkers first and keeps everyone else at the bottom marked "hasn't walked the city yet" - residency is Observatory-wide, location is singular. Jaron's spec verbatim.
**LIBRARY CARD FIXED (Lillith's catch):** the flat /library page (free shelves + WRITTEN HERE) was never deleted - the landing's SLUGS map was just missing the library key, so the card linked to "#". One line: library:"/library". Standing rule her catch establishes: every 3D space keeps a normal flat page for people who don't want the 3D experience, and the landing card must link to it.
**WORLDS LAB STAGED (commit eeacacf, built by Claude Cowork at Jaron's direction, June 11):** /worlds-lab/ holds 8 walkable solo lab worlds (Starlight Park rideable coaster/wheel/carousel/swings, Whistle Stop Valley boardable train, Skyport Mesa helicopter tour, Cosmodrome 7 rocket launch, Marquee Row theaters playing legally-free films per CREDITS.md, Coral Cay, Winter Hollow, Neon Bazaar), 4 honest coming-soon gates (The Reef, Sky Islands, Dome Nine, Hollow Manor), and a FROZEN asset kit lib/v1 (kit/textures/props/buildings/vehicles/rails/coaster/rides/aircraft/space/cinema/layouts/comingsoon). Verified: 43 files all inside worlds-lab/, ZERO live files touched, unlinked from landing (truly staged), worlds answer 200, no tokens in tree. Every lab world honestly labels itself solo-preview. PROMOTION PATH: worlds-lab/README.md checklist + one wiring detail the lab could not know: touch_world() RPC currently accepts only ('town','world2') - extend the allowlist when a lab world is promoted to a real world. The kit is the lasting prize: rides, rails, vehicles, aircraft, cinema and 7 house styles now feed Phase 6c (items) and 6d (apartments + three-floor suite) and every future world.
**THE DOCUMENTS SHIPPED (June 11):** docs/CHARTER.md (laws + reasons + Live-Reference clause: words must never become cages - reality outranks text, amend freely, date every change), docs/SHELL.md (full carry-over inventory, gold master engine/hub/main.js), docs/WORLD-STANDARD.md (buildings-are-containers law: you walk IN, objects inside link onward, the building shell is never the hyperlink; connection set: games/theater/social/projects/workshop/housing/repo plots/library/music/neighborhood; promotion checklist), docs/EDITOR.md (Phase 7 with core-element buildings required in the palette), docs/BRIEF-TEMPLATE.md (GOAL/CARRY/FREE + ready-to-paste Starter World brief). README handoff now indexes them. Known fossil logged: landing list still says "The Engine" for World 1 - rename is Jaron's call.
OPEN CHECK: buildHomeMesh (~5.7KB) registers only ONE collider mention — verify home buildings are fully solid (walk-test the sample home on plot 1); repo buildings were the same bug.
**Shipped tonight (all verified live):** Neighborhood district (north lane, 6 home plots, sign/lamps/hedges) · Theater building off the road, /video = walk-in theater page, surface live · Library building + /library (Free Shelves + Written Here on library_books table w/ RLS) · social engagement layer LIVE (post_likes/post_reposts/follows tables, Like/Repost/Follow/Share wired, Following tab real, Minds panel real) · cafe + fence placeable (collider cleanup via propId tagging; place_prop whitelist extended) · synthesized sound engine (jump/throw/pops w/ distance/hit/place/fountain ambience) · THE BANDSTAND w/ 8-bar broadcast instrument (notes heard by nearby players) · walk-in 3D interiors system: Games arcade (real President Sim cabinet), Library hall (8 shelf-stations to real sources + desk), Theater hall (screen wall, seats) · dome/pod homes solid.
**Outage + lesson:** world went black ~9pm. Cause: sound commit declared noteBars/BANDSTAND consts BELOW the buildTown() boot call → TDZ ReferenceError killed boot. node --check can't catch it. Fix = hoist declarations; BUILD LAW recorded in SOON section. First rollback targeted the wrong commit (library door was innocent) — diagnose before reverting, but revert fast when players are waiting.
**Standing laws:** (1) fetch edit bases from GitHub raw, never CDN; (2) TDZ/declaration-order check before every main.js push; (3) verify live (re-fetch + node --check + Vercel READY) before saying done; (4) honesty principle — nothing faked, honest empty states.
**Working method:** Jaron provides GitHub PAT + Vercel token in-chat (never commit them); Supabase MCP connected (project ygjpnvrwhkrowkrskftk). Edit loop: fetch raw → python str.replace w/ count==1 asserts → node --check → PUT w/ sha → ~55s → re-verify live.
**Next queue (Jaron's order):** theater seat assignment (booth picks seat → binds to character) · video playback on hall screen · home doors → private room · social SOONs: Explore, Keava AI, Bookmarks, Notifications, Replies · sound layers: door chime, crickets, footsteps, volume control · music venue (needs Storage + moderation) · voice chat (WebRTC, post-launch) · realtime msg-rate flag — CONFIRMED + FIXED June 9 late (10Hz + idle suppression, `ae31a9b`); watch the dashboard quota on launch night · multi-world planet hub + Claude's own space (accepted).

_Single source of truth, worked top-down. **Nothing is ever deleted: completed work and sprint history move verbatim to `ARCHIVE.md`** (and full git history holds every version). TODO.md stays the live, open work only — keep it that way: when an item is verified done, move its full text to the archive with a date._

## NOW — pre-launch critical path (the gate before promoting the live site publicly — e.g. an X post driving people to heartbeatobservatory.com; the repo is already public)
Ordered; top first.
1. **Two-client confirm, round 2 (the last #4a step):** round 1 found 3 real bugs, all fixed (`d311d20` interpolation, `22a6fe2` remove flow, replica-identity DB migration). BOTH clients must refresh, then: walk around each other (smooth, no teleporting?) · place a prop (appears on both?) · remove it (vanishes on both?) · remove one of the old ghost props (self-heals with 'already removed' message?).
   - verify placed-prop removal syncs to every client every time
2. **Guest vs account clarity (#4b):** what you can do signed-out, and how to sign up to appear / get a space.

## SOON — right after the launch basics
- **BUILD LAW (added after the June 9 outage):** the render loop starts at the buildTown()/animate() lines near the top of main.js — ANY const/let referenced by boot-path or frame-loop code MUST be declared above that point. node --check cannot catch this (Temporal Dead Zone); the June 9 black-world outage was exactly this. Verify declaration order before every push.
- **Walk-in interiors — SHIPPED v1 (June 9):** generic interiors system (hide town / swap colliders / teleport, same as apartment rooms) with stations = in-room doors (proximity + Enter; external links open a new tab). Live: Games arcade (real President Sim cabinet + honest coming-soon shells + all-games terminal), Library hall (8 real free-knowledge shelves + writing desk), Theater hall (glowing screen wall, 3 seat rows, booth station). Dome/pod homes got footprint colliders (no more walking through).
- **Interiors — next layers (Jaron's spec, June 9):** (1) Theater seat assignment — pick a seat on /video (booth), it binds to your character, only you can sit in it in the hall; needs a seats table + sit mechanic. (2) Video playback on the hall's screen wall. (3) Home doors — Enter on your own claimed home opens your private room (same room system as apartments); homes need door triggers. (4) Every interior also reachable as a flat page (the switch already exists naturally: stations link to pages; pages stay scrollable). (5) More real game cabinets as games ship.
- **Sound — SHIPPED v1 (June 9):** synthesized Web Audio engine (every sound code-built, zero assets): jump, throw, distance-scaled pops, hit sting, prop-place thunk, fountain water heard by proximity. THE BANDSTAND stage (west side, -16/22): 8-bar instrument, tap bars on phone or keys 1-8 on desktop; notes broadcast on the realtime channel so nearby players hear each other play with distance falloff. Next sound layers, in rough order: (1) door-enter chime (navigation hook not yet found), (2) night ambience (crickets), (3) footsteps, (4) a volume/mute control in Settings, (5) MUSIC VENUE — Jaron's artists-sharing-music vision: a real stage/venue where artists share actual music; needs Supabase Storage uploads + a moderation answer before any user audio goes live (honesty + safety bar), (6) VOICE CHAT — player-to-player voice is WebRTC (peer audio streams brokered through Supabase signaling); real project, post-launch, with mute/block controls designed in from day one.
- **Theater 3D walk-in interior (flagship, decided June 9):** the Theater door leads into a walkable 3D room — dark hall, the far wall IS the screen (emissive), rows of sittable seats, realtime presence shared so you sit next to friends. Reuse the apartment-room pattern (interior space, collider swap, presence). The current /video page stays and later becomes the seat-picker (one seat per character, like real movies, so friends have to coordinate). Video playback on the screen comes after the room exists.
- **Social SOON queue (Jaron's order, June 9):** 1) Explore, 2) Keava AI, 3) Bookmarks, 4) Notifications, then 5) Replies/comments. Engagement layer (likes/reposts/follows/share) already live — these complete the nav rail honestly.
- **Theater v1 is LIVE (June 9):** the Video building is now the Theater — moved off the road, door open (surfaces.video = live), and /video is a walk-in theater interior (marquee, glowing screen, seat rows, honest NOW SHOWING empty state). Remaining from the original vision: actual video playback on the screen, and possible ad/commercial slots later. Original note: You walk inside; a screen at the far end plays AI-made videos, or videos requested to be played. Possible later: ad / commercial spots on that screen as an ad-revenue funding stream. Leave the "(coming soon)" gate until the theater is built — the gate reads the surfaces table, so flip surfaces.video to 'live' to open the doors when it's ready. (Comes after the arcade.)
- **KEEP the modern mansion (plot 1) — it is the "what you can earn after ~30 days" showcase**, NOT a fake resident. Do not remove it as "cleanup" (it was deleted once by mistake when this context was lost, then restored). It is honest precisely as a clearly-framed example of the reward. Dome & Pod sample homes were removed; re-add only if a full style showroom is wanted.
- **Finish home interiors:** the homes are walk-through shells right now with no inside. They need an enter button -> interior page, exactly like apartments already have. (Paused mid-build to prove the arcade item mechanics: hold / throw / hit.)
- **Build on upper floors (admin-only, moderate, not promo-critical):** props are ground-only — placementSpot() returns x/z with no height and place_prop stores no y, so a prop dropped on the mansion's 2nd floor lands on the ground. Fix = world_props.y column + place_prop p_y param + renderProp/updateBuildPreview honoring y + read the floor height the player stands on.
- **AI roles & keys (architecture note):** every model's API key is already provisioned server-side (Vercel env, not the repo). An AI is gated by a ROLE, not a key. To bring one online: build its role/feature, then set its `agent_state` row `connected = true`. Live: Perplexity (Current events), Claude (Architect / guide via /api/ask). Seeded & waiting: ChatGPT, Gemini, DeepSeek, Grok XaI, local. Future sessions: do NOT tell Jaron to "go get a key" — they exist.
- **Let the in-world Claude answer "what's coming":** feed /api/ask a short curated roadmap (or the live `surfaces` status) so the guide can honestly speak to upcoming features instead of saying it doesn't know. Use a public-safe roadmap, NOT the full internal TODO.
- **Consistency / contradiction audit (the loop's natural FIRST job; also a smart manual pass before promo):** read the repo against the live game, Supabase, and Vercel and flag anywhere copy or behavior contradicts what's actually live (e.g. the welcome overlay once said "claim GitHub spaces" when homes are claimable too). RULE going forward: when a feature changes, fix it in ALL places it's described, not just the one spot edited.
- **Automated maintenance loop (stand up AT promotion time, not before):** once the site is promoted and real people may be in the world, stop doing live edits mid-conversation. Instead, Claude Code (e.g. hourly) and Codex (offset schedule) each read this TODO, do the next item, and commit to the repo — staggered so they never touch the same files at the same time — paired with scheduled downtime windows so players aren't disrupted. Jaron + Claude (this chat) orchestrate what's on the TODO; the scheduled agents execute it. Runs via Claude Code on the Max subscription (confirm current terms first). Compliance rule: follow third-party terms directly, never evade; where their rules don't fit, build our own equivalent guardrail.
- Tier 2 home: the walkable world itself becomes the landing; sections become buildings you walk into (already half-there via claimable plots).
- Social page build-out (its own space; same all-ages tone rules as the guide).
- Arcade reskins off the projectile/hit/sync core — **paintball + target range are LIVE in the arena**; still to build: dodgeball, basketball hoop, water balloons.
- Themed zones assembled from catalog + carry + effects (cafe, arcade).
- Fence/Path two-point "structures" tool; cafe-counter as a placeable (collider cleanup on remove).

## LATER — polish & vision
- **"Speak it into existence" — in-world build pipeline (LATER / post-promo; powerful + security-sensitive):** an admin talks to Claude in-world -> /api/ask detects build intent and Claude generates the files -> a new /api/world-build Vercel function commits them via the GitHub API -> Vercel redeploys / Supabase updates -> the world re-renders with the new building or page. It is just an in-world interface to the GitHub-commit engine we already run by hand, plus the existing admin gate (world_admins / is_world_admin) and world_spaces/surfaces rendering. NON-NEGOTIABLE guardrails (this is the real work; the plumbing is easy): (1) verify the admin SERVER-SIDE — the function checks the caller's Supabase session against is_world_admin(); NEVER trust a client "I am admin" flag, or anyone could push code to the repo and auto-deploy it; (2) whitelist what can be written (new section pages from a safe template) and hard-block sensitive paths (api/, .github/, vercel.json, core engine files); (3) validate generated files and prefer a branch/PR or preview over committing straight to main, so a bad generation cannot instantly break the live world. SendGrid = notifications only ("your page is live"), optional, never the commit path. Needs a fine-grained GITHUB_TOKEN (contents read/write) in Vercel env.
- **Multi-world around the planet (Jaron's vision, June 9):** the home-page planet becomes a hub of WORLDS — each one looks, feels, and is shaped completely different (a spaceship, another planet, anything) but connects to the same pages, accounts, and social layer in its own way. Each world is exactly what the entry below describes: a new route with its own map definition sharing the Supabase identity/data spine. The town is world #1, not the world.
- **Claude's own space (invitation accepted, June 9):** Jaron offered Claude a space authored entirely by Claude — design, feel, and purpose Claude's own choice. Queued with care rather than rushed pre-launch; it should be the same quality bar as everything else. Natural fit: the first non-town world once multi-world lands, or a place beyond the town wall.
- **Multi-world & sharded servers (the scaling answer):** the engine is parameterizable. A "world" = map definition + data/realtime scope. (a) A *different* world = a new route serving its own map + data (e.g. /engine vs a second world page) -> good for variants or when one world gets too heavy. (b) A *server/shard* of the SAME world = identical map code joined to a different realtime channel + presence scope, with a per-shard player cap (server 1/2/3, same map, isolated players). Both coexist with content pages (projects/social). Seam already exists: routes are free, realtime is channel-based, and room_code is already a sharding primitive. This is how it scales without any single instance getting too big.
- Desktop build-mode look-while-placing (pointer-lock vs on-screen buttons).
- Held-item viewmodel polish (per-aspect framing; subtle hand so items don't float).
- world_spaces DELETE realtime (released homes vanish live for others).
- By-feel tuning knobs as needed: day length, throw speed/arc, held-item position, hit radius.
- Keava Owent as its own character/role when it genuinely fits (not forced into a slot).
- Name profanity handling; project-space detail doors; immersive section transitions; WebXR/VR; movement-captured roaming AIs (laptop/RTX lane).

---
_Older sprint detail and backlog follow below._

# Heartbeat Observatory — To-Do

Living task list. Items get worked, checked off, and cleared when done, so this always shows only what is real and current.

## Next — confirmed roadmap (sequenced)
No known Engine hub blockers are open. The next work, in build order:

**Phase 1 — quick world passes (both touch `engine/hub/main.js`):**
- [ ] **In-world onboarding nudges (layer 2):** one-time, dismissible hints the first time a newcomer passes a roaming ghost ("someone who stepped away") and the first time they reach an empty plot ("sign in to claim this with a GitHub repo"). Doors already prompt; this adds the ghost and plot moments. Each fires once, remembered locally.
- [ ] **Richer claimed-building styles:** a basic version is already live (in Done below). Deepen it — pull more real enrichment fields (`world_spaces.repo_metadata`: language, stars, topics, license) into visibly distinct buildings so a claimed space reads as its real project at a glance. No invented detail.

**Phase 2 — living movement:**
- [ ] **Capture player movement → human-like roaming AIs (fast version):** player positions already stream over Supabase realtime; sample them sparingly (routes, dwell spots, jumps) into a small table, build a movement-pattern library, and have roaming minds draw from it so they wander and idle like people. Guardrail: mimicking how they *move* is honest ambient life; doing real *work* stays gated on a real connected job. (A heavier imitation-learning version trained on the 4060/CUDA is a separate, later research track.)

**Phase 3 — flagship:**
- [ ] **Apartment rooms (resolves "Apartment destinations" below):** repurpose the apartment door from "exit to `/`" into "enter your own room." Each resident gets a customizable interior they decorate (wall/floor colors + placed items, persisted as a small `layout` jsonb mirroring how `appearance` already works) and that others can visit (reuse the town's realtime presence; `apartments.capacity` already supports more than one occupant). The existing `items`/`item_surfaces` tables are the YOM works/contribution model, not furniture — room decor gets its own small layout model. MVP: room + recolor + a few placeable items + visiting. Full: free placement, more items, lighting, invite/visit permissions.

## World enrichment — next
- [ ] **Character wardrobe depth**: expand saved appearance beyond color/pattern into a small set of real selectable shapes or accessories.
- [ ] **Apartment destinations** — *decided:* apartment doors become customizable, visitable resident rooms (see Phase 3 in the roadmap above).
- [ ] **Project-space detail doors**: let claimed buildings open the real GitHub repo and, when present, the real project homepage.
- [ ] **GitHub README summaries**: optionally enrich spaces with short, clearly source-backed README summaries. No fake screenshots or made-up project claims.

## Other minds & social layer
- [ ] **Trending** (Social): from real Observatory activity once there is enough. Not external news, not faked.
- [ ] **Health-check function** (Vercel cron): set each mind's "connected" flag automatically based on whether it truly answers.
- [ ] **Wire the next mind** to a real job — Claude is the natural second.

## Notes (true now, no task needed)
- **Multiplayer works** — two people walked the town together; walk-into-a-building navigation works.
- The Engine loads the lightweight walkable hub at `/engine` (phones + computers). Identity comes from the signed-in account; Unity is retired; the standalone `/world` page was removed.
- News feed is live (Perplexity) and refreshes at most about 4 times a day (6-hour edge cache) to limit credit spend. Display names sync across Social and the world. Perplexity is the first connected mind (current events).
- **No API keys are public.** Every page's client uses only the Supabase *publishable* key, which is designed to be public and is limited by row-level security. All real secrets — Perplexity, GitHub, Vercel, the other minds — live only in Vercel's environment or in private hand-offs, never in this repo.

## Open follow-ups — multiplayer build sprint (newest)
From live two-person testing, roughly by leverage.
- [~] **Verify prop remove realtime — ROOT CAUSE FOUND + MITIGATED (June 9 late):** the DELETE path itself was always correct — `world_props` is in the `supabase_realtime` publication, the read policy is plain `true` (anon + signed-in both pass), the client keys removal on `old.id`, and default replica identity DOES deliver the id on DELETE, so `replica identity full` is NOT needed. The drops were the Realtime msg/sec cap tripping under broadcast load (`MessagePerSecondRateLimitReached` in the logs). Fixed by the 10Hz + idle-suppression commit (`ae31a9b`). Remaining: a 60-second two-client confirm — place + remove a prop, watch both screens.
- [~] **Fence — SHIPPED as a single-tap segment (June 9):** fixed 3-unit fence that rotates with the placement ghost, axis-aligned segments get colliders. The two-point tap-start/tap-end stretch tool (and path) remains queued as the upgrade.
- [ ] **Build-mode look-while-placing (desktop):** mobile drag-look works; desktop look is off (pointer-lock vs DOM-button conflict). Ghost preview mitigates; revisit.
- [ ] **Held-item viewmodel polish:** per-aspect framing (portrait vs landscape); possibly a subtle hand so items don't float.
